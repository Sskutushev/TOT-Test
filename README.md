## Обновления от 31 августа 2025 г. (Часть 3)

## Обновления от 1 сентября 2025 г.

В ходе текущей сессии были выполнены следующие задачи:

*   **Коррекция путей к изображениям:**
    *   Исправлены пути к изображениям для новостных карточек в `src/components/NewsSection.jsx`.
    *   Исправлен путь к изображению `Group google.svg` в приветственном баннере на главной странице (`src/pages/HomePage.jsx`).
    *   Изображение "Phone 1" в баннере приложения футера (`src/components/Footer.jsx`) было заменено с `Group 49.svg` на `Group google.svg` по уточнению пользователя.
*   **Подготовка к развертыванию и устранение проблем с кэшированием:**
    *   Проект был успешно собран (`npm run build`) для развертывания на GitHub Pages.
    *   Конфигурация `vite.config.js` подтверждена для вывода в папку `docs` с корректным базовым URL (`/TOT-Test/`).
    *   Для устранения проблемы с отображением старого изображения `Group 49.svg` на живом сайте была выполнена **чистая сборка** (удаление и повторное создание папки `docs`).
    *   Все изменения были зафиксированы и отправлены в удаленный репозиторий GitHub. Проблема с `Group 49.svg` успешно решена.

*   **Новая секция "Часто задаваемые вопросы" (FAQ):**
    *   На главную страницу лендинга добавлена последняя секция с часто задаваемыми вопросами.
    *   Реализована в виде интерактивного "аккордеона", где ответы открываются по клику на вопрос.
    *   Добавлено 10 карточек с вопросами и ответами, релевантными функционалу платформы.
*   **Новый компонент "Футер" (Подвал):**
    *   Создан и добавлен на все страницы лендинга глобальный футер.
    *   Футер содержит три колонки: с логотипом и юридической информацией, с меню навигации, и с контактами и баннером для скачивания приложения.
    *   Реализована анимация для баннера приложения при наведении.
*   **Полная адаптивность:**
    *   Реализована полная адаптивность для секции FAQ и Футера.
    *   **FAQ:** На мобильных устройствах колонки с вопросами перестраиваются в один столбец.
    *   **Футер:** На планшетах и мобильных устройствах колонки футера перестраиваются и центрируются для удобного отображения.

## Обновления от 31 августа 2025 г. (Часть 2)

*   **Адаптивность новых секций:**
    *   Реализована полная адаптивность для секций "Новости" и "План развития" для их корректного отображения на планшетах и мобильных устройствах.
    *   **Новости:** На маленьких экранах колонки с новостями и боковая панель перестраиваются в один вертикальный столбец.
    *   **План развития:** На планшетах карточки перестраиваются по две в ряд. На мобильных устройствах все элементы (вкладки, карточки) выстраиваются в один столбец для удобства просмотра. Размеры шрифтов и изображений динамически изменяются в зависимости от разрешения экрана.

## Обновления от 31 августа 2025 г.

*   **Новая секция "Новости":**
    *   На главную страницу лендинга добавлена секция с последними новостями.
    *   Секция состоит из четырех карточек новостей с заголовками, датами, кратким описанием и изображениями.
    *   Реализован попап (модальное окно) для просмотра полной статьи при клике на кнопку "Подробнее".
    *   Добавлен блок с призывом следить за обновлениями и видео-элементом.
*   **Новая секция "План развития" (Roadmap):**
    *   Добавлена интерактивная секция, демонстрирующая план развития проекта по четырем фазам.
    *   Реализована система вкладок ("Фаза 1" - "Фаза 4") для переключения между этапами.
    *   Каждая фаза содержит уникальный набор карточек с описанием ключевых задач и целей.
    *   Добавлены фоновые изображения и иконки для визуального оформления.
    *   Реализованы эффекты наведения на карточки и изображения.
*   **Исправления и улучшения:**
    *   Устранены несколько ошибок в верстке, включая критическую ошибку, приводившую к некорректному отображению карточек в секции "План развития".
    *   Проведены многочисленные итеративные корректировки позиционирования и стилей элементов в новых секциях на основе обратной связи.
    *   Исправлены синтаксические ошибки в коде, приводившие к сбою сервера разработки (Ошибка 500).

## Обновления от 29 августа 2025 г. (Часть 4)

*   **Финальные исправления и улучшения:**
    *   Устранен баг с некорректным отображением кнопок на мобильных и десктопных версиях в секции "Социальная сеть".
    *   Скорректированы отступы в карточках секции "Социальная сеть" для мобильных устройств.
    *   Унифицированы отступы в списках преимуществ в секциях "Образовательная платформа" и "Создавай, продавай...".
    *   Проведен финальный рефакторинг и чистка CSS для всех новых компонентов.

## Обновления от 29 августа 2025 г. (Часть 3)

*   **Глобальный рефакторинг кнопок:**
    *   В `index.css` добавлена глобальная система стилей для кнопок (`.btn`, `.btn-primary`) и цветовые переменные (`--primary-main`, и т.д.).
    *   Новая система включает стили для состояний `:hover`, `:active` и `:disabled`, а также плавные `transition` для анимаций.
    *   Все оранжевые кнопки на главной странице были переработаны для использования этой новой системы, что унифицировало их внешний вид и поведение.

*   **Создание новых секций:**
    *   Добавлена секция "Создавай, продавай, зарабатывай..." с текстовым блоком, списком преимуществ и видео.
    *   Добавлена сложная секция "Погрузись в мир..." с фоном из двух видео, сеточным макетом из 5 карточек и оверлеем.
    *   Для карточек в новых секциях добавлены эффекты наведения (увеличение и тень).

*   **Исправление багов и итеративные правки:**
    *   Устранен сложный баг с отображением кнопок в секции "Социальная сеть" на разных разрешениях экрана.
    *   Проведены многочисленные итеративные корректировки отступов и стилей в нескольких секциях по требованию.
    *   Реализована смена цвета иконки-стрелки при наведении в секции "Начни знакомство...".
    *   Исправлена проблема с переносом текста на кнопках.

*   **Полная адаптивность:**
    *   Реализована полная адаптивность для всех новых секций ("Создавай, продавай...", "Погрузись в мир..."), обеспечивающая корректное отображение на всех устройствах.

## Обновления от 29 августа 2025 г. (Часть 2)

*   **Исправление багов и рефакторинг:**
    *   Устранен баг с некорректным отображением и позиционированием кнопки в секции "Социальная сеть" на десктопных устройствах.
    *   Проведен рефакторинг всех кнопок на главной странице для использования единой глобальной системы стилей (`.btn`, `.btn-primary`), включая добавление эффектов `:hover`, `:active` и `:disabled`.
    *   Исправлена ошибка с переносом текста на кнопках.
    *   Исправлен заголовок в секции "Погрузись в мир...": убраны переносы и изменен цвет на черный.
    *   Скорректирован размер и позиционирование кнопки в секции "Создавай, продавай...".
    *   Кнопке "Присоединиться" в Hero-секции добавлены новые стили и корректная ссылка.

*   **Доработка иконок:**
    *   Реализована смена цвета иконки-стрелки при наведении на карточки в секции "Начни знакомство...".
    *   Добавлен эффект наведения (тень и увеличение) на сами карточки в этой секции.

*   **Полная адаптация новых секций:**
    *   Реализована полная адаптивность для секций "Создавай, продавай..." и "Погрузись в мир..." для их корректного отображения на планшетах и мобильных устройствах.
    *   Настроено корректное перестроение колонок и сеток в один столбец, изменены размеры и отступы элементов для мобильных экранов.

## Обновления от 29 августа 2025 г.

В рамках сессии была проделана следующая работа:

*   **Новая секция "Социальная сеть":**
    *   На главную страницу добавлена новая большая секция, посвященная социальной сети.
    *   Реализована сложная двухколоночная верстка, включающая текстовый блок, видео-презентацию и четыре информационные карточки с описанием преимуществ.
    *   Для одной из карточек добавлен специальный бейдж "В разработке".

*   **Адаптивность и рефакторинг:**
    *   Проведена полная адаптация новой секции "Социальная сеть" для корректного отображения на всех типах устройств.
    *   Изменена логика отображения заголовка в секции "Социальная сеть": на десктопе используются ручные переносы, на мобильных — автоматические.
    *   Кнопка "Подробнее" в той же секции перемещена в конец блока на мобильных устройствах для улучшения логики восприятия.
    *   Полностью переработана структура и стили кнопок в секции "Начни знакомство с миром ТОТ" на мобильных устройствах для решения проблем с отображением текста и улучшения компоновки.

*   **Итеративные правки верстки:**
    *   Внесены многочисленные итеративные корректировки отступов в секциях "Образовательная платформа" и "Начни знакомство с миром ТОТ" на основе детальной обратной связи для достижения желаемого визуального результата.

*   **Деплой:**
    *   Все изменения были успешно собраны и выгружены на GitHub Pages для публичного доступа.

# Проект TOT на React

Этот документ описывает текущую структуру проекта, проделанную работу по миграции со статических HTML-файлов на React и рекомендации по дальнейшей разработке.

## Обзор

Проект был успешно перенесен со статической верстки (HTML, CSS) на современный стек с использованием React. Это было сделано для повышения интерактивности, управляемости и масштабируемости приложения.

**Ключевые изменения:**

- **Архитектура:** Проект теперь является Одностраничным Приложением (SPA), управляемым React.
- **Навигация:** Вместо прямых переходов между HTML-файлами используется `react-router-dom` для плавной навигации без перезагрузки страницы.
- **Компонентный подход:** Каждая страница и переиспользуемые элементы (боковая панель, шапка) вынесены в отдельные React-компоненты.

## Процесс миграции

1.  **Инициализация:** Проект был создан с помощью `Vite`, современного и быстрого сборщика для веб-приложений.
2.  **Установка зависимостей:** Была установлена библиотека `react-router-dom` для управления маршрутизацией.
3.  **Структурирование:**
    - Все статические ассеты (папки `css` и `img`) были перенесены в папку `public`.
    - Создана стандартная для React-проектов структура в папке `src`: `components` (для переиспользуемых компонентов) и `pages` (для отдельных страниц).
4.  **Создание компонентов:**
    - Создан корневой компонент `App.jsx`, который управляет всей маршрутизацией.
    - Создан `Layout.jsx`, включающий общую боковую панель (`menupanel`) и верхнюю панель (`TopPanel`), который оборачивает все внутренние страницы сайта.
    - Создана новая страница `LandingPage.jsx` с кнопками "Войти" и "Регистрация".
5.  **Конвертация страниц:** Каждая HTML-страница (`index.html`, `timeline.html`, `marketplace.html` и т.д.) была преобразована в отдельный React-компонент в папке `src/pages`.

## Как запустить проект

Для запуска проекта в режиме разработки выполните следующие команды в корневой папке:

1.  **Установка зависимостей:**
    ```bash
    npm install
    ```
2.  **Запуск сервера разработки:**
    ```bash
    npm run dev
    ```

После этого приложение будет доступно по адресу `http://localhost:5173` (или по другому порту, указанному в терминале).

## Структура Проекта

- `public/` - Статические файлы. Сюда входят `css/styles.css` и все изображения `img/`.
- `src/` - Исходный код приложения.
  - `src/components/` - Переиспользуемые компоненты (`Layout.jsx`, `TopPanel.jsx`).
  - `src/pages/` - Компоненты, представляющие собой отдельные страницы (`HomePage.jsx`, `TimelinePage.jsx` и т.д.).
  - `src/data/` - Моковые (тестовые) данные.
  - `src/routes.js` - Конфигурация маршрутов.
  - `src/App.jsx` - Главный компонент, где настраивается роутинг.
  - `src/main.jsx` - Точка входа в React-приложение.
- `package.json` - Файл с описанием проекта, зависимостей и скриптов.

## Основные зависимости

- `react`: Основная библиотека для построения пользовательского интерфейса.
- `react-dom`: Связующее звено между React и DOM-деревом в браузере.
- `react-router-dom`: Библиотека для реализации маршрутизации в приложении.

## Стиль кода и дальнейшая работа

Для поддержания чистоты и консистентности кода рекомендуется придерживаться следующих правил:

- **Компоненты:** Создавать функциональные компоненты с использованием хуков (`useState`, `useEffect` и т.д.).
- **Стилизация:** Все стили должны быть вынесены в отдельные CSS-файлы, соответствующие компонентам. Использование инлайн-стилей не рекомендуется.
- **Новые страницы:** При добавлении новой страницы создавать для нее файл в `src/pages/` и прописывать новый маршрут в `src/routes.js`.
- **Переиспользуемая логика:** Сложную или повторяющуюся логику выносить в кастомные хуки (папка `src/hooks/`).
- **Данные:** Все моковые (тестовые) данные должны храниться в директории `src/data`.

## Руководство по стилю

### Цвета

- **Основной оранжевый:** `#FF7A00` (используется для кнопок, ссылок и акцентных элементов).
- **Основной черный:** `#333` (используется для основного текста).
- **Серый текст:** `#666` (используется для второстепенного текста).
- **Фон:** `white` (основной фон), `#FFF5EC` (фон для выделенных элементов).
- **Границы:** `#E3E3E3` (основной цвет границ).

### Кнопки

- **Основная кнопка:** класс `button-withicon` или `button-small-withicon`.
- **Кнопка настроек:** класс `settings-button`.
- **Кнопка выхода:** класс `logout-link`.

### Шрифты

- **Основной шрифт:** (не указан в коде, используется шрифт по умолчанию).
- **Размеры шрифтов:**
    - `1.4rem` (заголовки h2 в попапах)
    - `1.1rem` (заголовки h3 в дропдаунах)
    - `1rem` (заголовки h3 в секциях попапов)
    - `0.9rem` (основной текст, заголовки h4)
    - `0.8rem` (второстепенный текст).

### Другие элементы

- **Карточки уведомлений:** класс `notification-card`.
- **Выпадающие меню:** классы `notification-dropdown`, `profile-dropdown`.
- **Активная ссылка в меню:** класс `active-link`.

## Обновления от 27 августа 2025 г.

В ходе последней сессии была проведена масштабная работа по разработке и адаптации лендинговой страницы и страницы авторизации:

### 1. Лендинговая страница (Hero Section):
- **Хедер:** Полностью переработан адаптивный хедер с бургер-меню для мобильных и планшетных версий (появляется при ширине экрана < 1024px). Реализована логика открытия/закрытия меню, навигация и кнопки "Войти" / "Выбор языка" внутри меню.
- **Секция Hero:**
  - Создана сложная двухколоночная структура с текстом, кнопками, баннером и фоновым видео.
  - Реализована полная адаптивность секции Hero, включая:
    - Динамическую высоту (`calc(100vh - 70px)`) для отображения без прокрутки на разных устройствах.
    - Адаптивное масштабирование и позиционирование элементов (текста, баннера, видео) на различных брейкпоинтах (`1200px`, `1024px`, `768px`).
    - Корректное наложение слоев (текст поверх декораций, декорации поверх видео).
    - Точная настройка типографики (шрифты TT Travels Next Trl, Open Sans), размеров и цветов текста.
    - Адаптивное позиционирование декоративных элементов (треугольников и иконок) в баннере, включая их полупрозрачность на адаптациях.

### 2. Страница Авторизации (/login):
- **Полная реализация:** Создана двухпанельная страница авторизации с нуля.
- **Левая панель:**
  - Реализована адаптивная карусель изображений с автоматическим перелистыванием (5 секунд).
  - Добавлены динамические заголовки и подзаголовки для каждого слайда.
  - Внедрены кнопки "Вернуться" и "Выбор языка" (переиспользуемый компонент).
- **Правая панель:**
  - Содержит логотип, заголовок, кнопки входа через социальные сети (заглушки).
  - Реализованы переключаемые вкладки для ввода номера телефона или email.
  - Добавлена кнопка "Продолжить" с заданными размерами и закруглением.
  - Нижний текст с ссылками на правила и политику конфиденциальности, стилизованный и адаптированный.
- **Адаптивность:**
  - При ширине экрана < 900px левая панель скрывается, а правая занимает всю ширину.
  - Кнопки "Вернуться" и "Выбор языка" переносятся в верхнюю часть правой панели.
  - Весь контент правой панели центрируется по горизонтали.

### 3. Общие улучшения и Рефакторинг:
- **Компонент `LangSelector`:** Вынесен в отдельный переиспользуемый компонент, адаптированный для использования как в хедере, так и на странице авторизации (с учетом направления выпадающего меню).
- **Управление Ассетами:** Все пути к изображениям унифицированы и теперь указывают напрямую на корневую папку `public/`, что упрощает управление ресурсами.

### 4. Исправления и Доработки (после 27 августа 2025 г.):
- **Страница Авторизации (/login):**
  - Исправлена навигация кнопки "Продолжить": теперь она корректно перенаправляет пользователя на главную страницу приложения (`/home`).
  - Устранены ошибки импорта `useNavigate` и других компонентов, что приводило к сбоям на странице.
  - Проведена финальная доработка стилей кнопок-вкладок (центрирование текста, корректные отступы).
  - Исправлена проблема с отображением выпадающего меню выбора языка на странице авторизации (корректное позиционирование и фон).
- **Общие:**
  - Проведена финальная проверка и корректировка путей к ассетам (`Union.svg` и другие) в компонентах `CombinedDropdown.jsx` и `ProfileDropdown.jsx` для успешной сборки проекта.

## Recent Development Log (August 27, 2025 - Ongoing)

This section details the ongoing efforts to resolve deployment issues, particularly related to asset paths on GitHub Pages, and other encountered errors.

### Deployment Strategy Evolution:
- **Initial Goal:** Deploy directly from `main` branch to GitHub Pages root (`/`).
- **Challenge:** GitHub Pages requires assets to be prefixed with the repository name (`/TOT-Test/`) when deployed to a subpath.
- **Attempt 1 (Failed):** Set `base: '/'` in `vite.config.js` for both `serve` and `build`. This caused 404 errors for assets on GitHub Pages as they were requested from the domain root (`sskutushev.github.io/asset.svg`) instead of the repository subpath (`sskutushev.github.io/TOT-Test/asset.svg`).
- **Attempt 2 (Failed):** Reverted `base` to `base: '/TOT-Test/'` for `build` and `/` for `serve` in `vite.config.js`. This fixed the online asset paths but broke local development due to assets being requested from `/TOT-Test/` on `localhost`.
- **Attempt 3 (Failed):** Introduced `import.meta.env.BASE_URL` for all image references in JSX/JS. This was intended to dynamically resolve paths based on the `base` config. However, this led to `Rollup failed to resolve import` errors during build, as `import` statements cannot contain dynamic expressions like `import.meta.env.BASE_URL`. This also caused `Expected string but found "import"` syntax errors.

### Image Path Resolution (Current Approach):
- **Problem:** Vite's `base` option only prefixes URLs processed by its asset pipeline (e.g., CSS `url()`, imported assets). It does *not* automatically rewrite URLs for assets directly referenced with an absolute path (starting with `/`) in HTML or JSX.
- **Solution:** All direct image references in `.jsx` files (e.g., `<img src="/image.svg">`, `const img = "/image.svg"`) must be explicitly prefixed with `import.meta.env.BASE_URL`. This ensures paths are correctly resolved for both local development (where `BASE_URL` is `/`) and GitHub Pages deployment (where `BASE_URL` is `/TOT-Test/`).

### Errors Encountered and Fixes Applied:

1.  **`[plugin:vite:react-babel] ...: Unexpected token (5:23)` (Initial Error):**
    *   **Cause:** `import logoutIcon from import.meta.env.BASE_URL + 'Union.svg';` - Invalid syntax for `import` statement.
    *   **Fix:** Changed to `import logoutIcon from '/Union.svg';` (later reverted to `import.meta.env.BASE_URL` for direct usage).

2.  **`Uncaught ReferenceError: t4_img is not defined`:**
    *   **Cause:** Accidental removal of `t4_img` declaration during previous path fixes in `LandingHero.jsx`.
    *   **Fix:** Re-added `const t4_img = '/Union.svg';` (later updated to use `import.meta.env.BASE_URL`).

3.  **`Only one default export allowed per module` in `ProfileDropdown.jsx`:**
    *   **Cause:** Duplicate `export default ProfileDropdown;` statement.
    *   **Fix:** Removed the duplicate declaration.

4.  **`Rollup failed to resolve import "Union.svg"` (and other images):**
    *   **Cause:** Attempting to `import` static assets from the `public` folder directly into JavaScript/JSX files. Rollup expects these to be JavaScript modules.
    *   **Fix:** Removed `import` statements for static assets. Instead, assets are referenced directly in `src` attributes or string literals using `import.meta.env.BASE_URL + 'path/to/image.svg'`. 

5.  **`Expected string but found "import"` (e.g., `LoginPage.jsx` for `variant4.svg`):**
    *   **Cause:** Syntax error introduced by accidentally duplicating `img:` when trying to use `import.meta.env.BASE_URL` in a `src` attribute.
    *   **Fix:** Corrected the duplicated `img:` syntax.

6.  **Persistent 404 errors for assets on GitHub Pages (e.g., `rus.svg`, `TOT Logo.svg`, `styles.css`, `index-BW9duOSx.css`, `index-BtkE_liC.js`):**
    *   **Cause:** Mismatch between the `base` path configured in `vite.config.js` and how assets were referenced in the code. Vite's `base` option only prefixes assets processed by its pipeline; direct absolute paths (starting with `/`) in HTML/JSX are not automatically rewritten.
    *   **Fix:** Systematically updated all direct image/asset references in `.jsx` files to use `import.meta.env.BASE_URL + 'original_path_to_asset.ext'`. This ensures paths are correctly resolved for both local and deployed environments.

### Current Status:
- Localhost: All images and functionality should be working correctly.
- Online (GitHub Pages): The latest build with all these fixes has been pushed. Deployment status needs to be monitored.

## Обновления от 28 августа 2025 г.

В ходе сессии были решены критические проблемы с развертыванием и стилизацией проекта, а также добавлена и настроена новая секция на главной странице.

*   **Исправление Ошибок 404 при Развертывании:**
    *   Диагностирована и устранена основная причина ошибок 404 на GitHub Pages. Проблема заключалась в некорректной конфигурации сборки и отсутствии необходимых зависимостей для Tailwind CSS.
    *   Восстановлена правильная конфигурация `vite.config.js` и `index.html` для корректной работы как в режиме разработки, так и в производственной сборке.
    *   Пользователю была предоставлена инструкция по настройке GitHub Pages для развертывания из папки `/docs`.

*   **Полное Восстановление Стилей (Tailwind CSS):**
    *   Обнаружена и исправлена фундаментальная проблема, из-за которой все стили Tailwind CSS удалялись при сборке проекта.
    *   Созданы и настроены с нуля конфигурационные файлы `tailwind.config.js` и `postcss.config.js`.
    *   Установлены все необходимые зависимости для корректной работы Tailwind CSS с PostCSS (`tailwindcss`, `postcss`, `autoprefixer`, `@tailwindcss/postcss`).
    *   Впоследствии конфигурация была обновлена до использования современного плагина `@tailwindcss/vite`, что упростило процесс сборки.
    *   В главный файл стилей `src/index.css` были добавлены недостающие директивы `@tailwind`.
    *   Исправлена проблема с отсутствием глобальной таблицы стилей `public/css/styles.css` путем ее корректного подключения в `index.html`.

*   **Стилизация и Доработка Существующих Страниц:**
    *   **Страница входа (`/login`):** Проведены многочисленные итерации по настройке стилей:
        *   Скорректировано вертикальное выравнивание элементов в левой панели.
        *   Кнопки "Вернуться" и выбора языка были сначала обернуты в общий контейнер, а затем, по просьбе пользователя, стилизованы как отдельные закругленные элементы.
        *   Реализована логика для открытия выпадающего меню выбора языка вверх специально для этой страницы.
    *   **Главная страница (Hero Section):**
        *   Устранена проблема с обрезкой фонового видео путем изменения CSS-свойств `overflow` и `height`.
        *   Решена проблема наложения следующей секции на видео путем изменения позиционирования видеоконтейнера со `absolute` на `relative`, что вернуло его в основной поток документа.
        *   Убран разрыв между секцией Hero и следующей секцией.

*   **Создание и Настройка Новой Секции "Features":**
    *   С нуля создана новая сложная секция на главной странице под блоком Hero.
    *   Реализована верстка с использованием абсолютного позиционирования для декоративных элементов (эллипсов), фреймов и текстовых блоков в соответствии с детальными требованиями.
    *   Проведено несколько итераций по точной настройке положения, размеров, градиентных фонов и стилей текста всех элементов.
    *   Добавлена полная адаптивность для новой секции с использованием медиа-запросов, включая кардинальное изменение макета для мобильных устройств для обеспечения корректного отображения.
